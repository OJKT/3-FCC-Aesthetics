<!DOCTYPE html>
  <style>
    body {
      background: url(https://i.imgur.com/MJAkxbh.png)
    }
  </style>
# Created by https://www.gitignore.io/api/web,c++,windows,dotsettings,metaprogrammingsystem
# Edit at https://www.gitignore.io/?templates=web,c++,windows,dotsettings,metaprogrammingsystem

### C++ ###
# Prerequisites
*.d

# Compiled Object files
*.slo
*.lo
*.o
*.obj

# Precompiled Headers
*.gch
*.pch

# Compiled Dynamic libraries
*.so
*.dylib
*.dll

# Fortran module files
*.mod
*.smod

# Compiled Static libraries
*.lai
*.la
*.a
*.lib

# Executables
*.exe
*.out
*.app

### DotSettings ###
*.DotSettings

### MetaProgrammingSystem ###
workspace.xml
junitvmwatcher*.properties
build.properties

# generated java classes and java source files
#   manually add any custom artifacts that can't be generated from the models
#   http://confluence.jetbrains.com/display/MPSD25/HowTo+--+MPS+and+Git
classes_gen
source_gen
source_gen.caches

# generated test code and test results
test_gen
test_gen.caches
TEST-*.xml
junit*.properties

### Web ###
*.asp
*.cer
*.csr
*.css
*.htm
*.html
*.js
*.jsp
*.php
*.rss
*.xhtml

### Windows ###
# Windows thumbnail cache files
Thumbs.db
ehthumbs.db
ehthumbs_vista.db

# Dump file
*.stackdump

# Folder config file
[Dd]esktop.ini

# Recycle Bin used on file shares
$RECYCLE.BIN/

# Windows Installer files
*.cab
*.msi
*.msix
*.msm
*.msp

# Windows shortcuts
*.lnk

# End of https://www.gitignore.io/api/web,c++,windows,dotsettings,metaprogrammingsystem

<html>
  <p>Topics to be coverd will be bullet pointed and listed First: I will cover the Composition within boxes AKA text-align style elemtents or (object) property</p>
  <ul>
    <li>Create Visual Balance Using the text-align Property</li>
    <li>PassedAdjust the Width of an Element Using the width Property</li>
    <li>PassedAdjust the Height of an Element Using the height Property</li>
  </ul>
  <p>I will cover new tag elemnts which changes specified tags... by taging tags:</p>
  <ul>
    <li>Use the strong Tag to Make Text Bold</li>
    <li>Use the u Tag to Underline Text</li>
    <li>Use the em Tag to Italicize Text</li>
    <li>Use the s Tag to Strikethrough Text</li>
    <li>Create a Horizontal Line Using the hr Element</li>
  </ul>
  <p>I will also cover properties of tags using the style elements and ID attributes to my advantage</p>
  <ul>
    <li>Adjust the background-color Property of Text</li>
    <li>Adjust the Size of a Header Versus a Paragraph Tag</li>
    <li>Add a box-shadow to a Card-like Element</li>
    <li>Decrease the Opacity of an Element</li>
    <li>Use the text-transform Property to Make Text Uppercase</li>
  </ul>

    <style>
    h4 {
      text-align: center;
      background-color: rgba(45, 45, 45, 0.1);
      padding: 10px;
      font-size: 27px;
      text-transform: uppercase;
    }
    p {
      text-align: justify;
    }
    .links {
      text-align: left;
      color: black;
      opacity: 0.7;
    }
    #thumbnail {
      box-shadow: 0 10px 20px rgba(0,0,0,0.19), 0 6px 6px rgba(0,0,0,0.23);
    }
    .fullCard {
      width: 245px;
      border: 1px solid #ccc;
      border-radius: 5px;
      margin: 10px 5px;
      padding: 4px;
    }
    .cardContent {
      padding: 10px;
    }
    .cardText {
      margin-bottom: 30px;
    }
  </style>
  <!-- <p>Take in the elements I have used in the paragraph below I have removed any <s>strikethroughs</s> marked as shown.</p> --> 
  <div class="fullCard" id="thumbnail">
    <div class="cardContent">
      <div class="cardText">
        <h4>OJKT_STYLE</h4>
        <hr>
        <p><em>Style: a true sense of cool, is to not be cool. calm in heated situations, nothing is ever too much for someone with <u>style</u> and just like the <strong>Mona Lisa</strong> they have characatures and characteristics such as that smirk.</em></p>
      </div>
      <div class="cardLinks">
        <a href="https://en.wikipedia.org/wiki/Larry_Page" target="_blank" class="links">Larry Page</a><br><br>
        <a href="https://en.wikipedia.org/wiki/Sergey_Brin" target="_blank" class="links">Sergey Brin</a>
      </div>
    </div>
  </div>
  
   <style>
    h1 {
      font-size: 68px;
      font-weight: 600;
      
    }
    h2 {
      font-size: 52px;
      font-weight: 500;
      position: absolute;
      top:10px;
      left:10px
    }
    h3 {
      font-size: 40px;
      font-weight: 400;
    }
    h4 {
      font-size: 32px;
      font-weight: 300;
    }
    h5 {
      font-size: 21px;
      font-weight: 250;
    }
    h6 {
      font-size: 14px;
      font-weight: 150;
    }
    p {
      position: relative;
      bottom: 10px;
    }
  </style>
  <ul>
    <li><h1>Set the font-size for Multiple Heading Elements</h1></li>
    <li><h2>Set the font-weight for Multiple Heading Elements</h2></li>
    <li><h3>Set the font-size of Paragraph Text</h3></li>
    <li><h4>Set the line-height of Paragraphs</h4></li>
    <li><h5>Adjust the Hover State of an Anchor Tag</h5></li>
    <li><h6>keep calm and code on</h6></li>
  </ul>
  <img src="https://i.imgur.com/eWWi3gZ.gif" alt="a gif to show how offset elements work to a objext text such as a heading.">
  <legend>
    <p>How Ofset Elements work... In a Gif<p>
  </legend>
  <hr>
  <body>
  <p>The CSS box model is just the way the elements follow the normal flow... we can change the normal flow with the style element, see the last style section above and how I have changed this paragraph with position relative. Also see how I have used position Asolute on the earlier h2</p>
<hr>
//example code//
<!--
   <style>
    #navbar {
      position: fixed;
      top: 0px;
      left: 0px;
      width: 100%;
      background-color: #767676;
    }
    nav ul {
      margin: 0px;
      padding: 5px 0px 5px 30px;
    }
    nav li {
      display: inline;
      margin-right: 20px;
    }
    a {
      text-decoration: none;
    }
  </style>
  <body>
    <header>
      <hr>
      <h1>Welcome!</h1>
      <nav id="navbar">
        <ul>
          <li><a href="">Home</a></li>
          <li><a href="">Contact</a></li>
        </ul>
      </nav>
    </header>
    <p>I shift up when the #navbar is fixed to the browser window.</p>
  </body>
-->
  <hr>
  <p><u>You can also float boxes for navigation. left and right</u> below is an example of overlapping and using z-index to order boxes</p>
  <style>
    div {
      width: 60%;
      height: 200px;
      margin-top: 20px;
    }
    .first {
      background-color: red;
      position: absolute;
      z-index: 2;
    }
    .second {
      background-color: blue;
      position: absolute;
      left: 40px;
      <!--<p>You can see the margin-top was set to 20px, it could also be set horizontally to auto. This method works for images, too. Images are inline elements by default, but can be changed to block elements when you set the display property to block.</p>--> 
      top: 50px;
      z-index: 1;
    }
  </style>
  <div class="first"></div>
  <div class="second"></div>
//example code//
  <p>Colours have several characteristics including hue, saturation, and lightness. CSS3 introduced the hsl() property as an alternative way to pick a color by directly stating these characteristics.</p>
  <hr>
  <p>after looking at HEX codes and short HEX it is important to introduce HSL... as catogorised into three as explained above... HSL uses a colour wheel and the angles from the 12'O'clock position round 360DEG. hue uses a color wheel concept instead of the spectrum, where the angle of the color on the circle is given as a value between 0 and 360.</p>
  <ul>
    <li>red	hsl(0, 100%, 50%)</li>
    <li>yellow	hsl(60, 100%, 50%)</li>
    <li>green	hsl(120, 100%, 50%)</li>
    <li>cyan	hsl(180, 100%, 50%)</li><!--slight adjustments say 10% of both saturation and lightness can change the colour dramatically-->
    <li>blue	hsl(240, 100%, 50%)</li>
    <li>magenta	hsl(300, 100%, 50%)</li>
  </ul>
  <p>You can use this colour wheel to get any color you wish! above is some basic colours in HSL</p>
  <hr>
  <h1><strong>GET HYPED<strong></h1>
  <hr>
  <p>you can use the CSS transform scale property to; change size, scale an element on hover, skew on element in different axis'
  //example code//
  <style>
  div { 
    width: 70%;
    height: 100px;
    margin:  50px auto;
    background: linear-gradient(
      53deg,
      #ccfffc,
      #ffcccf
    );
  }
  div:hover {
  transform: scale(1.1);
  }
</style>
<div></div>
//example code//
  <style>
    .ball { 
      width: 40px;
      height: 40px;
      margin: 50 auto;
      position: fixed;
      background: linear-gradient(
        35deg,
        #ccffff,
        #ffcccc
      );
      border-radius: 50%;
    }
    #ball1 {
      left: 20%;
    }
    #ball2 {
      left: 65%;
      transform:scale(1.5);
      transform: skewX(24deg);<!--
    }
    </style>
  <div class="ball" id= "ball1"></div>
  <div class="ball" id= "ball2"></div>
    <hr>
    <p>[7] see below</p>
    <ul>
      <li> a Graphic Using CSS</li>
      <li>Create a More Complex Shape Using CSS and HTML</li>
      <li>Learn How the CSS @keyframes and animation Properties Work</li>
      <li>Use CSS Animation to Change the Hover State of a Button</li>
      <li>Modify Fill Mode of an Animation</li>
      <li>Create Movement Using CSS Animation</li>
      <li>Create Visual Direction by Fading an Element from Left to Right</li>
      <li>Animate Elements Continually Using an Infinite Animation Count</li>
      <li>PassedMake a CSS Heartbeat using an Infinite Animation Count</li>
      <li>Animate Elements at Variable Rates</li>
      <li>Animate Multiple Elements at Variable Rates</li>
      <li>Change Animation Timing with Keywords</li>
      <li>Learn How Bezier Curves Work</li>
      <li>Use a Bezier Curve to Move a Graphic</li>
      <li>Make Motion More Natural Using a Bezier Curve</li>
    </ul>
    <hr>
  
  <style>
  .stars {
    background-color: white;
    height: 30px;
    width: 30px;
    border-radius: 50%;
    animation-iteration-count: infinite;
  }

  .star-1 {
    margin-top: 15%; 
    margin-left: 60%;
    animation-duration: 1s;
    animation-name: twinkle;
  }
  </style>
  
  <@keyframes twinkle {
    20% {
      transform: scale(0.5);
      opacity: 0.5;
    }
  <!--<p>note how the key frames is used for any animation or action</p>-->
  <hr>
  <p>once you have made a graphic such as a cresent or more complex shapes such as cars, horses and figures... using @keyframes you can change any aspect of the text or boxes at "key frames" in the animation specified by %...100% is the end frame and 0% is the start frame... you can change scale,hover state, color, opacity, variable rates, and animation timings... there are more complicated mathematical changes you can make... a quick example below</p> 
  <p>
   animation-timing-function: cubic-bezier(0, 0, 0.58, 1);
                           : Linear
                           : ease-out
                           :>there are many other timing functions that follow the flow of any mathical graph... you will have to google them if you need them I supose<I'm sure there will be someother github user with repositories fully dedicated to theese functions as well as colors and style elements and (ect)
  </p>                                            
<html>
